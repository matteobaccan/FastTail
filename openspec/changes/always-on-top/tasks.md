## 1. Implementation

- [ ] 1.1 Add `always_on_top: bool` to `FastTailConfig` with `to_ini` / `from_ini` and a round-trip test
- [ ] 1.2 Title bar pin button, Settings checkbox and Ctrl+Shift+T sending `ViewportCommand::WindowLevel`; apply the saved state on the first frame
- [ ] 1.3 i18n keys (tooltip, setting) in five languages; i18n test

## 2. Docs

- [ ] 2.1 README shortcut table and feature list
